defmodule Shop.Website.Content do
  @moduledoc "Customer-owned public content. Query this application's database here."
  # This runs per request. Publishing never replaces this module or operational records.
  def load, do: %{}
end
